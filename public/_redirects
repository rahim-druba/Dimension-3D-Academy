# Serve static assets (JS, CSS, images, 3D models) - must come BEFORE catch-all
/assets/*  /assets/:splat  200
/pics/*    /pics/:splat    200
/glb/*     /glb/:splat     200
/scg/*     /scg/:splat     200

# SPA fallback - all other routes serve index.html
/*    /index.html   200
